---
title: "Crédito Rural por CAR (SICOR)"
api: "POST /v1/certidoes/sicor_creditos_car"
description: "Operações de crédito rural registradas no SICOR (Banco Central) vinculadas ao imóvel por código CAR: produto, valor financiado, juros, área e status (vigente ou vencido), com resumo agregado."
---

## Descrição

Operações de crédito rural registradas no SICOR (Banco Central) vinculadas ao imóvel por código CAR: produto, valor financiado, juros, área e status (vigente ou vencido), com resumo agregado.

A consulta é **síncrona**: a API processa e retorna os dados estruturados e,
quando houver, os arquivos oficiais (PDF) com **URL de download assinada
(validade de 30 minutos)**.

## Autenticação

Envie sua API key no header `Authorization: Bearer SEU_TOKEN`. Cada chamada
bem-sucedida consome **1 unidade** da cota mensal. Em erro de consulta a cota
é estornada.

## Parâmetros

<ParamField body="codigo_car" type="string" required>
  Código SICAR do imóvel.
</ParamField>

## Resposta

<ResponseField name="certidao" type="string">
  Identificador da certidão consultada (`sicor_creditos_car`).
</ResponseField>

<ResponseField name="data" type="object">
  Payload estruturado do resultado (varia conforme a certidão).
</ResponseField>

<ResponseField name="arquivos" type="array">
  Lista de arquivos oficiais. Cada item traz `filename`, `content_type`,
  `kind`, `url` (assinada) e `expires_in` (segundos até expirar - 1800 = 30 min).
</ResponseField>

<RequestExample>

```bash cURL
curl -X POST "https://data.dadosfazenda.com.br/v1/certidoes/sicor_creditos_car" \
  -H "Authorization: Bearer SEU_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "codigo_car": "MG-3111507-0718293A4B5C6D7E8F90A1B2C3D4E5F6"
  }'
```

</RequestExample>

<ResponseExample>

```json 200 OK
{
  "success": true,
  "message": "Success",
  "data": {
    "certidao": "sicor_creditos_car",
    "data": { },
    "arquivos": [
      {
        "filename": "sicor_creditos_car.pdf",
        "content_type": "application/pdf",
        "kind": "official_pdf",
        "url": "https://data.dadosfazenda.com.br/storage/v1/object/sign/certidoes/...",
        "expires_in": 1800
      }
    ]
  }
}
```

```json 401 - API key inválida
{ "detail": "API key invalida." }
```

```json 429 - cota mensal atingida
{ "detail": "Limite mensal atingido. Reseta no proximo mes." }
```

</ResponseExample>
