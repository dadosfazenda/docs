---
title: "Florestas públicas por CAR"
api: "GET /v2/car/{codigo_car}/sobreposicoes/florestas-publicas"
description: "Cadastro Nacional de Florestas Públicas (SFB) sobre o imóvel"
---

## Descrição

Cadastro Nacional de Florestas Públicas (SFB) sobre o imóvel. Informe o código do CAR: a resposta diz se há ocorrência, traz cada uma com seus atributos, a área de interseção com o imóvel e o polígono em GeoJSON.

Para saber apenas SE existe ocorrência (sem os detalhes), use o [resumo de sobreposições](/api-reference/v2/sobreposicoes-resumo), que responde todas as camadas de uma vez.

<ParamField path="codigo_car" type="string" required>
  Código do CAR. Exemplo: `GO-5211909-40A31261C76845DCBE6C47797F9B7B57`
</ParamField>

<ParamField query="geometria" type="boolean" default="true">
  Incluir o polígono de cada ocorrência. Com `false`, a resposta fica muito menor em camadas densas.
</ParamField>

<ParamField query="limite" type="integer" default="200">
  Máximo de ocorrências (1 a 1000). Ao atingir o limite, a resposta traz `truncado: true`.
</ParamField>

## Resposta

<ResponseField name="codigo_car" type="string">Código do CAR consultado</ResponseField>
<ResponseField name="camada" type="string">Nome da camada</ResponseField>
<ResponseField name="descricao" type="string">Descrição da camada</ResponseField>
<ResponseField name="total" type="integer">Quantidade de ocorrências que intersectam o imóvel</ResponseField>
<ResponseField name="area_sobreposta_ha" type="number">Soma das áreas de interseção da camada com o imóvel</ResponseField>
<ResponseField name="itens" type="array">
  Ocorrências encontradas
  <Expandable title="item">
    <ResponseField name="area_sobreposta_ha" type="number">Área da interseção desta ocorrência com o imóvel, em hectares (não a área total da ocorrência)</ResponseField>
    <ResponseField name="geometry" type="object">Geometria em GeoJSON (EPSG:4326). Omitida com `geometria=false`</ResponseField>
    <ResponseField name="atributos" type="object">
      Atributos da ocorrência, com os nomes da base pública de origem
      <Expandable title="atributos">
      <ResponseField name="nome" type="string"></ResponseField>
      <ResponseField name="categoria" type="string"></ResponseField>
      <ResponseField name="protecao" type="string"></ResponseField>
      <ResponseField name="tipo" type="string"></ResponseField>
      <ResponseField name="governo" type="string"></ResponseField>
      <ResponseField name="comunitari" type="string"></ResponseField>
      <ResponseField name="classe" type="string"></ResponseField>
      <ResponseField name="orgao" type="string"></ResponseField>
      <ResponseField name="sobreposic" type="string"></ResponseField>
      <ResponseField name="atolegal" type="string"></ResponseField>
      <ResponseField name="anocriacao" type="string"></ResponseField>
      <ResponseField name="estagio" type="string"></ResponseField>
      <ResponseField name="codigo" type="string"></ResponseField>
      <ResponseField name="observacao" type="string"></ResponseField>
      <ResponseField name="uf" type="string"></ResponseField>
      <ResponseField name="bioma" type="string"></ResponseField>
      <ResponseField name="geocodigo" type="integer"></ResponseField>
      <ResponseField name="municipio" type="string"></ResponseField>
      <ResponseField name="shape_leng" type="number"></ResponseField>
      <ResponseField name="shape_area" type="number"></ResponseField>
      <ResponseField name="area_ha" type="number"></ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
<ResponseField name="truncado" type="boolean">Presente quando o retorno atingiu o `limite`</ResponseField>

<RequestExample>
```bash cURL
curl "https://data.dadosfazenda.com.br/v2/car/GO-5211909-40A31261C76845DCBE6C47797F9B7B57/sobreposicoes/florestas-publicas" \
  -H "Authorization: Bearer $API_KEY"
```
</RequestExample>

<ResponseExample>
```json 200 - sucesso
{
  "success": true,
  "message": null,
  "data": {
    "codigo_car": "GO-5211909-40A31261C76845DCBE6C47797F9B7B57",
    "camada": "florestas-publicas",
    "descricao": "Cadastro Nacional de Florestas Públicas (SFB) sobre o imóvel",
    "total": 1,
    "area_sobreposta_ha": 55.5275,
    "itens": [
      {
        "area_sobreposta_ha": 55.5275,
        "geometry": {"type": "MultiPolygon", "coordinates": [[[[-51.90, -17.40], [-51.89, -17.40], [-51.89, -17.41], [-51.90, -17.40]]]]},
        "atributos": {
          "nome": "texto",
          "categoria": "texto",
          "protecao": "texto",
          "tipo": "texto",
          "governo": "texto",
          "comunitari": "texto",
          "classe": "texto",
          "orgao": "texto",
          "sobreposic": "texto",
          "atolegal": "texto",
          "anocriacao": "texto",
          "estagio": "texto",
          "codigo": "texto",
          "observacao": "texto",
          "uf": "texto",
          "bioma": "texto",
          "geocodigo": 123,
          "municipio": "texto",
          "shape_leng": 12.34,
          "shape_area": 12.34,
          "area_ha": 12.34
        }
      }
    ]
  }
}
```

```json 401 - sem chave de API
{ "detail": "Não autenticado" }
```

```json 404 - CAR não encontrado
{ "success": false, "message": "CAR não encontrado", "data": null }
```

```json 422 - código de CAR inválido
{ "success": false, "message": "Código de CAR inválido", "data": null }
```
</ResponseExample>
