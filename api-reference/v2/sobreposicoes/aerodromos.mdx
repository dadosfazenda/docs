---
title: "Aeródromos por CAR"
api: "GET /v2/car/{codigo_car}/sobreposicoes/aerodromos"
description: "Aeródromos (ANAC) dentro do imóvel"
---

## Descrição

Aeródromos (ANAC) dentro do imóvel. Informe o código do CAR: a resposta diz se há ocorrência, traz cada uma com seus atributos e o polígono em GeoJSON.

Para saber apenas SE existe ocorrência (sem os detalhes), use o [resumo de sobreposições](/api-reference/v2/sobreposicoes-resumo), que responde todas as camadas de uma vez.

<ParamField path="codigo_car" type="string" required>
  Código do CAR. Exemplo: `GO-5211909-40A31261C76845DCBE6C47797F9B7B57`
</ParamField>

<ParamField query="geometria" type="boolean" default="true">
  Incluir o polígono de cada ocorrência. Com `false`, a resposta cai de megabytes para kilobytes em camadas densas.
</ParamField>

<ParamField query="limite" type="integer" default="200">
  Máximo de ocorrências (1 a 1000). Quando atinge o limite, a resposta traz `truncado: true`.
</ParamField>

## Resposta

<ResponseField name="codigo_car" type="string">Código do CAR consultado</ResponseField>
<ResponseField name="camada" type="string">Nome da camada</ResponseField>
<ResponseField name="descricao" type="string">Descrição da camada</ResponseField>
<ResponseField name="total" type="integer">Quantidade de ocorrências que intersectam o imóvel</ResponseField>
<ResponseField name="itens" type="array">Ocorrências encontradas</ResponseField>
<ResponseField name="itens[].geometry" type="object">Geometria em GeoJSON (EPSG:4326). Omitida com `geometria=false`</ResponseField>
<ResponseField name="itens[].atributos" type="object">Atributos da ocorrência, conforme a base de origem (campos abaixo)</ResponseField>
<ResponseField name="truncado" type="boolean">Presente quando o retorno bateu no `limite`</ResponseField>

### Atributos da ocorrência

Os nomes seguem a base pública de origem.

<ResponseField name="itens[].atributos.openaip_id" type="string"></ResponseField>
<ResponseField name="itens[].atributos.name" type="string"></ResponseField>
<ResponseField name="itens[].atributos.icao_code" type="string"></ResponseField>
<ResponseField name="itens[].atributos.iata_code" type="string"></ResponseField>
<ResponseField name="itens[].atributos.alt_identifier" type="string"></ResponseField>
<ResponseField name="itens[].atributos.type" type="integer"></ResponseField>
<ResponseField name="itens[].atributos.type_label" type="string"></ResponseField>
<ResponseField name="itens[].atributos.country" type="string"></ResponseField>
<ResponseField name="itens[].atributos.elevation_m" type="number"></ResponseField>
<ResponseField name="itens[].atributos.magnetic_declination" type="number"></ResponseField>
<ResponseField name="itens[].atributos.traffic_type" type="string"></ResponseField>
<ResponseField name="itens[].atributos.ppr" type="boolean"></ResponseField>
<ResponseField name="itens[].atributos.private_use" type="boolean"></ResponseField>
<ResponseField name="itens[].atributos.skydive_activity" type="boolean"></ResponseField>
<ResponseField name="itens[].atributos.winch_only" type="boolean"></ResponseField>
<ResponseField name="itens[].atributos.runways" type="object"></ResponseField>
<ResponseField name="itens[].atributos.frequencies" type="object"></ResponseField>
<ResponseField name="itens[].atributos.services" type="object"></ResponseField>
<ResponseField name="itens[].atributos.hours_of_operation" type="object"></ResponseField>
<ResponseField name="itens[].atributos.images" type="object"></ResponseField>
<ResponseField name="itens[].atributos.telephone_services" type="object"></ResponseField>
<ResponseField name="itens[].atributos.contact" type="string"></ResponseField>
<ResponseField name="itens[].atributos.remarks" type="string"></ResponseField>
<ResponseField name="itens[].atributos.source_created_at" type="string"></ResponseField>
<ResponseField name="itens[].atributos.source_updated_at" type="string"></ResponseField>
<ResponseField name="itens[].atributos.attrs" type="object"></ResponseField>

<RequestExample>
```bash cURL
curl "https://data.dadosfazenda.com.br/v2/car/GO-5211909-40A31261C76845DCBE6C47797F9B7B57/sobreposicoes/aerodromos" \
  -H "Authorization: Bearer $API_KEY"
```
</RequestExample>
